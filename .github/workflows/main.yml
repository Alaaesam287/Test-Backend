name: Security Scans

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: true

      - name: Create sarif output directory
        run: mkdir -p sarif

      # ── gosec ───────────────────────────────────────────────────────────────
      - name: Run gosec (all severities)
        continue-on-error: true
        id: gosec
        run: |
          go install github.com/securego/gosec/v2/cmd/gosec@latest

          gosec \
            -fmt=sarif \
            -out=sarif/gosec.sarif \
            -format=json -out=sarif/gosec.json \
            ./...

          # Count ALL issues (no severity filter)
          ISSUES=$(jq '.Issues | length' sarif/gosec.json 2>/dev/null || echo 0)
          echo "issues=$ISSUES" >> $GITHUB_OUTPUT
      # ── govulncheck ─────────────────────────────────────────────────────────
      - name: Run govulncheck
        continue-on-error: true
        id: govulncheck
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest

          govulncheck \
            -format=sarif \
            ./... > sarif/govulncheck.sarif || true

          govulncheck -format=json ./... > sarif/govulncheck.json || true

          # Count found vulnerabilities
          VULNS=$(jq '.Vulns | length' sarif/govulncheck.json 2>/dev/null || echo 0)
          echo "vulns=$VULNS" >> $GITHUB_OUTPUT

      # ── Upload to Security tab ─────────────────────────────────────────────
      - name: Upload SARIF results
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: sarif/     # uploads all .sarif files in this folder

      # ── Job summary ─────────────────────────────────────────────────────────
      - name: Create job summary
        if: always()
        run: |
          echo "## 🔒 Security Scan Results" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**gosec** (high severity): $(jq '.Issues | length' sarif/gosec.json 2>/dev/null || echo 0) issues" >> $GITHUB_STEP_SUMMARY
          echo "**govulncheck**: $(jq '.Vulns | length' sarif/govulncheck.json 2>/dev/null || echo 0) vulnerabilities" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Details → Security → Code scanning tab" >> $GITHUB_STEP_SUMMARY
          echo "Full JSON reports → Artifacts (security-reports)" >> $GITHUB_STEP_SUMMARY

      # ── Artifacts (only JSON + SARIF for debugging/review) ─────────────────
      - name: Upload reports as artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: sarif/*.json sarif/*.sarif
          retention-days: 14

      # ── Block if issues found (skip on manual dispatch) ────────────────────
      - name: Fail if security issues found
        if: |
          github.event_name != 'workflow_dispatch' &&
          (steps.gosec.outputs.issues != '0' || steps.govulncheck.outputs.vulns != '0')
        run: |
          echo "::error::Security issues found in gosec or govulncheck."
          echo "→ Review in Security → Code scanning"
          echo "→ Download artifacts for JSON/SARIF details"
          exit 1